// Import and re-export all shared styles
import './ZingModalStyles.css'
import './ZingTransactionsModalStyles.css'
import './ZingProjectModalStyles.css'

// Export any JavaScript-related styling utilities here if needed
// For example:
export const applyDarkMode = (element) => {
  element.classList.add('dark-mode')
}

export const removeDarkMode = (element) => {
  element.classList.remove('dark-mode')
}

// Add more style-related utilities as needed
